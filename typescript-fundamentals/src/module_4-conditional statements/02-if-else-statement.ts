// if else statement
// Used when we want one block of code to run if condition is true, otherwise another block should run
let actualTitle = 'Login Page';
let expectedTitle = 'Dashboard';

if (actualTitle === expectedTitle) {
  console.log('Title validation passed');
} else {
  console.log('Title validation failed');
}
