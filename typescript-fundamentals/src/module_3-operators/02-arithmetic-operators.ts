// Arithmetic operators
// Arithmetic operators are used to perform mathematical operations on numeric values
// The most common arithmetic operators are addition (+), subtraction (-), multiplication (*), division (/), remainder/modulus (%), and exponentiation (**)
let a = 10;
let b = 3;

console.log(a + b); // 13 - addition
console.log(a - b); // 7  - subtraction
console.log(a * b); // 30 - multiplication
console.log(a / b); // 3.333... - division
console.log(a % b); // 1 - remainder/modulus
console.log(a ** b); // 1000 - exponent/power, 10^3

// ---------------------------------------------------------------------------

// QA example
let totalTests = 100;
let failedTests = 8;
let passedTests = totalTests - failedTests;

console.log(passedTests); // 92
